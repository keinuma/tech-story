type Story {
    id: Int!
    title: String!
    user: User!
}

type User {
    id: Int!
    uuid: String!
    name: String!
    description: String
}

input NewStory {
    text: String!
    userId: Int!
}

input NewUser {
    uuid: String!
    name: String!
    description: String
}

type Query {
    getStories: [Story!]!
}

type Mutation {
    createStory(input: NewStory!): Story!
    createUser(input: NewUser!): User!
}
